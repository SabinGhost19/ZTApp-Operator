# Generated gRPC stubs land here at Docker build time (see Dockerfile).
# Kept empty in source control because the .py output of protoc is verbose
# and tightly coupled to the grpcio/protobuf versions in requirements.txt.
